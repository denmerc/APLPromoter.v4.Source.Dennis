<UserControl x:Class="APLPromoter.UI.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
        <Border CornerRadius="6"
	        BorderBrush="Black"
	        BorderThickness="1"
	        Background="White"
	        Padding="4"
	        Margin="6">
            <Border.Effect>
                <DropShadowEffect Opacity=".5" />
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="UserName:" />
                <TextBox x:Name="userName" />
                <TextBlock Text="Password:" />
                <TextBox x:Name="password" />
                <!--<TextBox x:Name="message" BorderBrush="White" IsReadOnly="True" IsEnabled="False"/>-->
                <Label x:Name="message" BorderBrush="White" Background="White" Height="30"  IsEnabled="true" Margin="-4,0,4,0"/>
                <Button x:Name="login" Content="Login"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
